{
  "private": true,
  "name": "workout-monorepo",
  "scripts": {
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run start",
    "dev": "npx concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run start\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
